apiVersion: batch/v1
kind: Job
metadata:
  name: free
spec:
  template:
    spec:
      containers:
      - name: free
        image: jasonyoungsim/mp12:latest
        command: ["python", "classify.py"]
        resources:
          requests.cpu: "900m"
        env:
          - name: APP_ENV
            value: development
          - name: DATASET
            value: mnist
          - name: TYPE
            value: ff
      restartPolicy: Never
  backoffLimit: 4